'use client';

interface StickyMobileBarProps {
  onOpenQuote?: () => void;
}

// Sticky mobile bar disabled to eliminate clutter and button fatigue on mobile viewports
export default function StickyMobileBar({ onOpenQuote }: StickyMobileBarProps = {}) {
  return null;
}
